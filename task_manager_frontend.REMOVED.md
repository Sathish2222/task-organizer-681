# task_manager_frontend folder removed

The previous `task_manager_frontend/` directory has been removed because the Next.js app now lives at the repository root (`task-organizer-681/`).

If you are looking for the frontend source, see:

- `task-organizer-681/app/`
- `task-organizer-681/package.json`
- `task-organizer-681/next.config.js`

Environment variables should be configured via the container `.env` at:

- `task-organizer-681/.env`

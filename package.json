{
  "name": "task-organizer-681-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Task manager frontend (Next.js) moved to repo root.",
  "scripts": {
    "dev": "next dev -p ${PORT:-3000}",
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
